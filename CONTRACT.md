# Search Contract — exclusion-display-audit (PUBLIC; frozen at setup)

**Campaign kind:** search (counterexample/existence — not optimization)

**Objective:** For each of Borde (CQG 4, 1987, 343), Graham-Olum (PRD 76, 064001) and Wall (PRD 81, 024038) independently: exhibit one exact, premise-satisfying instantiation x of a DERIVED displayed relation R_i such that P_i(x) holds and R_i(x) fails under the paper's printed conventions and expansion order, re-verified by the frozen mechanical predicate; or, absent any such object, ship the per-paper exact-confirmation certificates and the named obstruction for every unresolved row (QUESTION-STATUS outcome). Only derived rows are targets; definitions, premises and imported results are inputs.

**Search space:** A candidate is {paper, row, instance, conventions?}. The row registry is frozen inside verifier.py (ROWS) from the preprint bytes read 2026-09-18 (GO arXiv:0705.3193v2 pp.2-4, seven displays GO-1..7 plus prose note GO-P1; Wall arXiv:0910.5751v2 pp.5-16, displays W-4..W-34). Borde has NO rows: only the abstract is public (IOPscience paywall confirmed this session); its rows enter through `har extend-search` after the Director acquires and freezes the inventory. Instance families per evaluable derived row (exact arithmetic only, rational/closed-form data, no floats): GO-5 {Omega: positive rational constant, a: constant (e.g. 1/(2880*pi^2)), T_kk(lam), Z_kk(lam): closed-form integrable functions of the affine parameter}; W-7 {dimension 4, coords, 4x4 metric and tangent field k as RATIONAL functions of the coordinates, point with rational coordinates}: the verifier itself computes Christoffels, Ricci, B_ab = nabla_b k_a, theta, sigma^2, omega^2, d theta/d lambda and G_kk = R_kk at the point, so the candidate supplies geometry only; W-17-18, W-24, W-25 {rational entropy/area increments}; W-27, W-30, W-31 {formal series coefficients theta0, theta_half, theta1, sigsq0, sigsq_half, sigsq1, T0_kk, T1_kk, R1_kk as functions of lam; the verifier rebuilds W-7 as a series in eps = hbar^(1/2) and extracts the printed order}; W-29, W-33 {T1_kk(lam), sigsq1(lam) with convergent line integrals; the verifier solves the ODE with the W-28 boundary conditions}. Calibration rows FW-D7 (printed 12 pi, secondary transcription from Sienkiewicz 2026 Remark 8.1) and FW-D7-corrected (16 pi) exercise the checker: the verifier independently derives the kernel constant 2 c'(2) = 16 pi from c(nu) = 2^(nu+2) pi Gamma(1+nu/2)/Gamma(-nu/2) via the reflection formula, plus an mpmath control on h = r^4 exp(-r^2) (momentum integral over pi must equal 16 pi). Mechanism families (all four priced at the analytic pass, per the pitch): F1 representation (handled by the pinned-convention gate: any declared convention other than the printed one is refused), F2 finite algebra (W-7 coefficient 1/2, contractions; the W-7 evaluator is proven sensitive by the coefficient-discrimination fixture), F3 integration/limits (GO-5 convergence, W-29/33 convergence and sign decidability; non-convergent data abstains), F4 premise transport (dimension unstated in Wall: D != 4 abstains; W-23 unitarity identification, W-26 interval quantifier, W-34 species remainder: abstain, PARK triggers). Invariances: none quotiented; fingerprint = (row, canonicalized instance), conservative. Candidate templates and their order freeze before the first calculation (pitch); per-row budget 1 symbolic closure + at most 32 exact instantiations.

**Constraints:** verify(candidate) returns valid=True only when: the row exists in the frozen inventory and is kind=derived with a mechanical evaluator; any declared conventions equal the pinned printed conventions (signature -+++, MTW Ricci, 8 pi G T = G, G = 1 in Wall, affine null k, theta = nabla_a k^a, hbar expansion with half orders); every premise of the row is independently established on the instance (constant positive Omega and convergent T_gamma, J_gamma for GO-5; symmetric nondegenerate Lorentzian metric at the point, k null and affinely geodesic identically, twist-free at the point, D = 4 for W-7; W-15/W-16/W-23 inequalities for the entropy rows; theta0 = sigma0 = T0_kk = 0 on H, no hbar^(1/2) terms consistent with W-13, and W-7 satisfied as a formal identity through order hbar for the series rows; convergence, ODE and boundary conditions W-28 and the sign premise W-26/W-32 for the ODE rows); and the printed relation evaluated exactly at the printed order fails (nonzero residual, or an inequality violated). Definitions, premises, imported results and prose refuse; derived rows without an evaluator abstain with the named missing operation; evaluator errors abstain. Nothing candidate-supplied is used as the relation: R and P come from ROWS only. Circular instantiation (defining the transformed stress by GO-3 and checking GO-3) is structurally impossible because GO-3 is imported and not a target. Exact confirmations are reported as valid=False with reason 'exactly confirmed' and are separate audit artifacts, never hits.

**Verifier:** `verifier.py` — `verify(candidate) -> {'valid': bool, 'reason': str}`

**Tools:** python3.12, sympy==1.14.0, mpmath==1.3.0 (numeric control only, never decides), pydantic==2.13.4 (harness side)

**Kill criteria:** DRAFT NOT MACHINE-RUN: the drafting session's backend denied every python invocation (Bash and subagent). Setup must not proceed until the Director runs `python verifier.py --fixtures` and `--witnesses` and every fixture reports ok (FW 12 pi hits, 16 pi confirms, W-7 planar witness confirms, coefficient-discrimination control detects 1 vs 1/2). Any fixture mismatch is a draft defect: repair and re-review before freeze.; PARK a paper when an authoritative source, reconciled classification or source-faithful evaluator is unavailable at the end of its setup allocation. Borde is parked at draft time (no public full text); Wall parks unless the Director rules on W-23 (unitarity identification of regions), W-26/W-32 (interval quantifier with implicit continuity) and W-34 (unquantified inter-sector remainder), which have no mechanical evaluator; W-10..W-12 are definitional bookkeeping and need a Director ruling on whether they count as derived rows.; Whole-paper completion requires zero unresolved derived rows; instantiation agreement or search exhaustion never substitutes for a symbolic certificate. GO-5, W-7, W-17-18, W-24, W-25, W-27, W-29, W-30, W-31, W-33 are identities or linear consequences whose symbolic closure is the certificate; the search on them is a checker exercise and expected null.; Stop the evaluator if any mandatory positive control fails, any false-discrepancy fixture (convention, definition, satisfied strict inequality, missing premise, beyond-order) is accepted, a source or code pin changes, or the independent reimplementation disagrees.; Budget: 0 exploratory search runs, at most 1 final certificate-replay run, 1 island, 480 s clock, max_revisions 0, $0 cloud; 24 role sessions total per the approved pitch. Verify() must run under 100 ms per candidate for the algebraic rows and under 5 s for W-7 (symbolic Ricci); a W-7 candidate exceeding 5 s abstains and parks that route.; Applicability finding to carry, not a hit: W-7 prints the D = 4 coefficient 1/2 while the dimension is not printed (footnote 9 contemplates extra dimensions); downstream consequence none (used at theta = sigma = 0 and linearized). GO-P1: printed 'Omega of order exp(2880 pi^2)' versus 1/(8a) = 360 pi^2 from the paper's own GO-5; within one decade, no consequence.; Person-facing outputs (author emails) remain Director-gated; the 12 pi kernel constant must be pinned from Flanagan-Wald's primary bytes before any calibration claim is quoted outside the harness.

**Max search runs:** 1

**Ship condition:** Either (a) one verified hit on a derived row of a target paper, re-verified by the unchanged predicate and by a fresh source-based reimplementation, or (b) all three papers CLOSED or PARKED with per-row certificates/obstructions and the assumption-provenance map (geometry vs matter per step, recorded in the row registry's `consumes` field); (b) is a first-class QUESTION-STATUS outcome.

**Rejection examples (verifier must refuse these):** `{"paper": "W", "row": "7", "instance": {"dimension": 4, "coords": ["t", "r", "x"`; `{"paper": "FW", "row": "D7-corrected", "instance": {}} -> refused: independent k`; `{"paper": "GO", "row": "4", "instance": {}} -> refused: GO-4 is a definition`; `{"paper": "W", "row": "29", "instance": {"T1_kk": "-exp(-lam^2)"}} -> refused: p`; `{"paper": "W", "row": "7", "conventions": {"signature": "+---"}, "instance": {..`

_The verifier is the oracle. A hit is valid=True, nothing else._
